<div class="container mt-4">
  <div class="row">
    <h4>ログイン</h4>
  </div>
  <div class="row mt-4">
    <h4 class="offset-sm-1">会員の方はこちらからログイン</h4>
  </div>
  <div class="row mt-4">
    <div class="col-sm-10">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource  %>
        <div class="form-group row">
          <%= f.label :"メールアドレス", class: "offset-sm-2 col-sm-3" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "col-sm-5" %>
        </div>

        <div class="form-group row">
          <%= f.label :"パスワード", class: "offset-sm-2 col-sm-3" %>
          <%= f.password_field :password, autocomplete: "current-password", class: "col-sm-5" %>
        </div>

        <div class="row mt-5">
          <%= f.submit "ログイン", class: "btn btn-primary col-sm-2 font-weight-bold offset-sm-5" %>
        </div>
      <% end %>
    </div>
  </div>
  <div class="row mt-5">
    <h4 class="offset-sm-1">会員登録がお済みでない方</h4>
  </div>
  <div class="row mt-3">
    <p class="offset-sm-3"><%= link_to "こちら", new_session_path(resource_name) %>  からログインしてください</p>
  </div>
</div>
</div>

